import java.util.Scanner;

public class MaxOfThreeNumbers{
    public static void main(String[] args) {
        try(Scanner sc  = new Scanner(System.in)){
            // System.out.print("Enter NUmbers : ");
            // int a = sc.nextInt();
            // int b = sc.nextInt();
            // int c = sc.nextInt();
            // int d = sc.nextInt();
            // System.out.println(Math.max(Math.max(a, b),Math.max(c,d))+" is maximum in numbers in all numbers");
            // System.out.println(Math.min(Math.min(a, b),Math.min(c,d))+" is minimum in numbers in all numbers");

            System.out.println(Math.pow(0, 0));
        }

    }
}